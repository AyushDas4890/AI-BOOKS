# Build dependencies for The AI/ML Interview Book
# Install with: pip install -r requirements.txt

# PDF compilation pipeline
weasyprint>=60.0
markdown>=3.5
Pygments>=2.17

# Mathematical rendering (SVG via mathtext)
matplotlib>=3.8

# Code listing execution dependencies (for running/verifying chapter code)
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
